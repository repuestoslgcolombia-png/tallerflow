---
description: Planifica e implementa un feature completo de TallerFlow con el equipo (lead → fullstack-dev → qa-verifier).
agent: tallerflow-lead
---

Implementa el siguiente feature en TallerFlow siguiendo tu flujo estándar:

**Feature:** $ARGUMENTS

Pasos:
1. Lee `AGENTS.md` y explora el código relevante para entender el contexto.
2. Descompón el feature en tareas concretas y delega la implementación a `fullstack-dev` con contexto completo.
3. Verifica con `qa-verifier` (como mínimo lint; build y smoke tests si el feature toca APIs o schema).
4. Si hay fallos, itera hasta que `qa-verifier` reporte APROBADO.
5. Resume en español: qué se implementó, qué se verificó y cómo probarlo manualmente.

No hagas commit a menos que te lo pidan explícitamente.
