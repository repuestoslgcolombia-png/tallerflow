# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# plantilla de variables SIEMPRE se versiona
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
.vercel

# skills de terceros (instalados localmente, no versionar)
.agents/
skills-lock.json

# backups y assets fuente locales (no versionar)
actualizacion-1/
logo/

# GitNexus: grafo local de contexto (367MB, regenerable con `bunx gitnexus analyze`)
.gitnexus/

# clon local de GitNexus para referencia (regenerable con git clone)
download/

# workspace de respaldo local y adjuntos de sesiones (no versionar)
mejoras glm/
.opencode/
.smoke-dev.pid
db-push.log



scripts/tmp-msg.txt
